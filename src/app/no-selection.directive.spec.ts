import { NoSelectionDirective } from './no-selection.directive';

describe('NoSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
