import { RequestInterceptor } from "./request-interceptor";

describe('HttpInterceptor', () => {
  it('should create an instance', () => {
 /*    expect(new RequestInterceptor()).toBeTruthy(); */
  });
});
